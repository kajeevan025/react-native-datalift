engine-strict=true

# GitHub Packages — scoped to this org/user
# Token is supplied via NODE_AUTH_TOKEN env var at publish time
# (set automatically by GitHub Actions setup-node, or export locally)
@kajeevan025:registry=https://npm.pkg.github.com
